int main(){
    if( 1 < 2 ){
        Print("one is less than two");
    }
    if( 1 <= 2 ){
        Print("one is less than or equal to two");
    }
    if( 2 > 1 ){
        Print("two is greater than one");
    }
    if( 2 >= 1 ){
        Print("two is greater than or equal to one");
    }
}